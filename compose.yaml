services:
  server:
    build:
      context: .
      target: final
    env_file:
      - .env
    volumes:
      - ./database:/app/database
    environment:
      - DB_PATH=/app/database
## No ports yet
#    ports:
#      - 8080:8080
## Currently no option for external database
#     depends_on:
#       db:
#         condition: service_healthy
#   db:
#     image: postgres
#     restart: always
#     user: postgres
#     secrets:
#       - db-password
#     volumes:
#       - db-data:/var/lib/postgresql/data
#     environment:
#       - POSTGRES_DB=example
#       - POSTGRES_PASSWORD_FILE=/run/secrets/db-password
#     expose:
#       - 5432
#     healthcheck:
#       test: [ "CMD", "pg_isready" ]
#       interval: 10s
#       timeout: 5s
#       retries: 5
# volumes:
#   db-data:
# secrets:
#   db-password:
#     file: db/password.txt

# TODO: add option for external database
# Issue URL: https://github.com/electricsteve/RustDiscordBot/issues/18
